# Dependencias y builds
node_modules/
dist/
build/
.expo/
.expo-web-test/
web-build/

# Entorno
.env
.env.local
.env.*.local

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Claves de deploy (no subir)
gh-regalo-magico-deploy
gh-regalo-magico-deploy.pub

# Respaldo accidental
node_modules.bak/

# Catálogo import temporal
app-web/bd/catalog.import.json

# Manual de usuario (local / Overleaf — no subir al repo)
docs/

# Despliegue VPS y CI con datos sensibles (solo local)
app-web/DEPLOY-EQUIPO1.md
app-web/.github/workflows/deploy.yml

# Variables de entorno locales (copiar desde .env.example)
.env
.env.local
.env.*.local
app-web/.env
app-movil/.env
