# Catálogo solo local (sin backend):
# VITE_USE_API=false

# Con backend: Vite reenvía /api al proxy (por defecto 8081 en el host).
VITE_USE_API=true
VITE_API_BASE_URL=/api

# Si el API Docker escucha en otro puerto del host:
# VITE_DEV_API_PROXY=http://127.0.0.1:8081
